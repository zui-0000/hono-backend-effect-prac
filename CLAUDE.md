# CLAUDE.md

このリポジトリで作業するときの規約。
設計そのものの規約と背景は [README](README.md) と [`docs/`](docs/) にある。

**コンテキストを跨ぐ話が出たら、先に [`docs/★境界と越境/`](docs/★境界と越境/) を読む。**
何を渡してよくて何を渡してはいけないか、その理由、迷った判断の記録がある。

## コミット

Conventional Commits（`feat` / `fix` / `refactor` / `chore` + スコープ）。

- **破壊的変更マーカー `!` は、`schema/`（API 契約）が変わってクライアントが壊れる場合にだけ付ける。**
  判定基準を「外部契約が壊れるか」に固定しておくと迷いようがない。
  例: `feat(api)!: envelope パターンを廃止し、応答をフラットにする`
- 内部のリネームや構造変更（バレル廃止、`features/` → `contexts/` など）は、
  **どれだけ広範囲でも `!` を付けない** — 直すのは自分たちだけだから。
- semantic-release 等は入れていないため `!` を読むのは人間だけ。機械的な効果はない。

### 進め方

- **コミットとプッシュは指示されたときだけ行う。** 作業が一段落しても勝手にコミットしない。
- コミット前に `pnpm lint:fix` と `pnpm test` を通す
  （`lint:fix` は lint → 整形 → 型チェック → 依存構造検査を順に走らせる）。
- コミットメッセージには「何をしたか」だけでなく**なぜそうしたか**を書く。
  検討して見送った案があれば、それも残す（同じ調査を繰り返さないため）。

## 検証

- **lint ルールを追加・変更したら、わざと違反するファイルを作って確認する。**
  検出されることと、**許可すべきものが通ること**の両方を確かめてから消す。
  「書いたのに効いていない」は実際に何度も起きている
  （詳細は [`docs/03-boundary-enforcement.md`](docs/03-boundary-enforcement.md#落とし穴)）。
- API を変えたら、テストだけでなく**実 DB を立てて通しで叩く**
  （`docker compose up -d` → `pnpm db:migrate` → `pnpm start`）。

## ドキュメント

- doc コメントは**散文**で書き、JSDoc タグ（`@param` / `@returns` / `@throws`）は使わない。
  型は TypeScript が、失敗は Effect の `E` チャネルが語るので、タグの出番がない。
  コメントが足すべきは**なぜ**の部分だけ。
- 設計判断は該当する `docs/NN-*.md` に追記する。README は「動かすために要る情報」に留める。
  ただし**コンテキストを跨ぐ判断で迷ったとき**は、結論だけでなく
  「なぜそちらに倒したか」を [`docs/★境界と越境/迷った判断/`](docs/★境界と越境/迷った判断/) に
  1 ファイルとして残す（ファイル名は日本語で、迷った問いをそのまま書く）。
