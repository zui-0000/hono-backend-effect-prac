/**
 * API のエラーコード体系。
 * `<HTTP ステータス><連番>` の 4 桁で、下 1 桁 0 が各ステータスの汎用エラー、
 * 1 以降が具体的な事由を表す専用エラー。
 *
 * **先頭 3 桁は「作られたときの HTTP ステータス」であって、現在の値ではない。**
 * ステータスを変える判断をしても、コードは動かさない。事由が変わっていないのに
 * クライアントの分岐が壊れるため。実際 `ForbiddenError` は 404 に畳む形で一度実装し、
 * その後 403 へ戻している（[`docs/02-architecture.md`](../../../../docs/02-architecture.md) の認可の節）。
 * あのとき仮に「規則に忠実に」コードも付け替えていれば、事由が 1 つも変わっていないのに
 * フロントの分岐が 2 度壊れていた。
 *
 * 裏を返すと、**先頭 3 桁は将来ズレうる**。読み替えの根拠が要るときは HTTP の
 * ステータス行を見ること。ここは識別子であって状態ではない。
 *
 * **ここが値の登録簿。** TypeSpec 側の `ErrorCode` は `scalar ... extends string` で、
 * 値そのものは列挙していない（各エラーモデルの `@example` に現れるだけ）。
 *
 * **専用コードを足すのは「クライアントが分岐する必要のある事由」だけ。**
 * 4091 (メールアドレス重複) は、画面に「別のアドレスを入れて」と出すために
 * 汎用の 409 と区別が要る。一方、経路の打ち間違いのように**誰も分岐しないもの**は
 * 汎用のまま返す。コードを増やすと契約の周知とクライアント実装の手間が乗るので、
 * 増やす側に理由が要る。
 */
export const ErrorCode = {
  /** 400 リクエスト内容が不正 (汎用) */
  BadRequest: "4000",
  /** 401 認証情報が不正 (汎用) */
  Unauthorized: "4010",
  /** 403 操作する権限が無い (汎用) */
  Forbidden: "4030",
  /** 404 リソースが存在しない (汎用。経路の打ち間違いもここに含める) */
  ResourceNotFound: "4040",
  /** 409 リソースの現在の状態と衝突する (汎用) */
  Conflict: "4090",
  /** 409 メールアドレスが既に使用されている */
  MailAddressDuplication: "4091",
  /** 500 サーバー内部で予期せぬエラーが発生した (汎用) */
  InternalServerError: "5000",
} as const;

export type ErrorCode = (typeof ErrorCode)[keyof typeof ErrorCode];
